[package]
name = "kaman-perf"
description = "Profiling and frame-timing instrumentation for KamanEngine."
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[features]
default = []
# Enable the stdout HUD / performance-summary printing. Off by default so the
# macOS smoke oracle keeps clean output; timing/rolling-average tracking is
# always available regardless of this flag.
perf-hud = []

[dependencies]
